<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/training_details_nav_graph"
    app:startDestination="@id/trainingDetailsFragment">

    <fragment
        android:id="@+id/trainingDetailsFragment"
        android:name="com.kolsanovafit.feature.training.details.presentation.fragment.details.TrainingDetailsFragment"
        tools:layout="@layout/fragment_training_details">

        <argument
            android:name="trainingId"
            app:argType="integer" />
        <argument
            android:name="description"
            app:argType="string" />
    </fragment>

</navigation>